<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "DevJobMappr" %></title>
    <link href='http://fonts.googleapis.com/css?family=Kreon' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
      <nav class="top-bar" data-topbar>
    <ul class="title-area">
      <!-- Title Area -->
      <div class="app">
            JobMappr
      </div>
      <li class="toggle-topbar menu-icon"><a href="#"><span>menu</span></a></li>
    </ul>
 <section class="top-bar-section">
<%= link_to_modal 'New Job', new_job_path, :class=>"button [radius round] left job" %> 
<% if user_signed_in? %>
<ul class="right">
    <li class="has-dropdown">
        <a href="">
Hi <%= current_user.email %></a>
        <ul class="dropdown">
          <li> <%= link_to "Dashboard", jobs_path, :class=>"button right" %>
            
          </li>
          <li> <%= link_to "Sign Out", destroy_user_session_path,:method => :delete, :class=>"button right" %>
</li>
        </ul>
      </li>
    </ul>

<% else %>
<%= link_to_modal "Sign Up", new_user_registration_path, :class=>"signup" %>
<%= link_to_modal "Sign In", new_user_session_path %>


<% end %>
</section>
</nav>
    <%= javascript_include_tag "application" %>

    <%= yield %>
  </body>
</html>