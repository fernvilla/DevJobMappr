<h1>Jobs#index</h1>
<p>Find me in app/views/jobs/index.html.erb</p>

<%@jobs.each do |job| %>
<%= job.job_title %>
<%= job.latitude %>
<%= job.longitude %>
<%= link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>

<%= link_to_modal 'New Job', new_job_path %>